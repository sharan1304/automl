apiVersion: v1
kind: Service

metadata:
  name: automl-service

spec:
  type: NodePort

  selector:
    app: automl

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30007